{
  "name": "sql-formatter-standardized",
  "version": "1.0.0",
  "dependencies": {
    "sql-formatter": "^13.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "scripts": {
    "build": "esbuild main.js --bundle --outfile=dist/bundle.js && cp index.html dist/"
  }
}
