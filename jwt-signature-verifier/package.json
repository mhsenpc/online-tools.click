{
  "name": "jwt-signature-verifier",
  "version": "1.0.0",
  "description": "JWT Signature Verifier - 100% client-side JWT signature verification using Web Crypto API",
  "scripts": {
    "build": "esbuild main.js --bundle --outfile=dist/bundle.js && cp index.html dist/"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}